---
kind: pragmatic
type: heuristic
domain: [agentic-engineering, coding-agents, context-engineering]
confidence: 0.75
sources: 1
entities: [Claude Code, Anthropic]
refs: ['https://code.claude.com/docs/en/best-practices']
---
# After two failed corrections, clear context rather than correcting a third time

A concrete threshold worth adopting: if you have corrected an agent more than twice on the same issue in one session, stop correcting. The context now contains the failed approaches, which actively bias the next attempt toward them. Clear and restart with a better initial prompt incorporating what you learned. A clean session with a sharper prompt almost always beats a long session carrying accumulated corrections.

The same principle drives the other named failure patterns:
- The kitchen-sink session — unrelated tasks interleaved, context full of irrelevance. Clear between unrelated tasks.
- Infinite exploration — an unscoped "investigate X" reads hundreds of files and consumes the window. Scope it, or delegate to a subagent whose context is separate.
- Trust-then-verify gap — plausible-looking code that misses edge cases. If you cannot verify it, do not ship it.

The unifying idea is that context is the constrained resource and correction history is a low-value occupant of it. Practitioners tend to treat clearing as losing progress; it is usually removing an obstacle.
