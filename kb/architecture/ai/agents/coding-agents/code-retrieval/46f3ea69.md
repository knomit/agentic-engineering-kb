---
type: observation
domain: [agentic-engineering, coding-agents, context-engineering, evaluation]
confidence: 0.7
sources: 1
entities: [Sourcegraph, CodeScaleBench, Kubernetes, code search]
refs: ['https://sourcegraph.com/blog/why-coding-agents-fail-large-codebases', 'https://sourcegraph.com/blog/codescalebench-testing-coding-agents-on-large-codebases-and-multi-repo-software-engineering-tasks']
---
# Coding agents degrade past roughly 400K LOC, and it is a retrieval-infrastructure problem rather than a model-capability one

Sourcegraph scored 1,281 agent runs across 40+ large open-source repositories in 9 languages (their CodeScaleBench benchmark) and reports a size threshold: agents degrade systematically once a codebase exceeds roughly 400,000 lines. Below that, local file reading and grep are adequate; above it, the agent navigates file references without converging.

The headline measurement is a Kubernetes task that took 6,000+ seconds (90+ minutes) with local file access and 89 seconds with indexed code search — "Same model. Same task. Different context infrastructure." Adding code-intelligence tools was worth +0.259 reward in the 400K–2M LOC band. Cross-file refactoring went from touching 2 of 7 affected files (score 0.32) to all 7 (0.80). The multi-repository retrieval delta was +0.209 F1 versus +0.085 for single-repo, so the benefit scales with how scattered the relevant code is.

The actionable claim: if your agent is failing on a large codebase, the first thing to change is the retrieval layer (indexed search, go-to-definition, find-references, type hierarchies), not the model or the prompt. Structural navigation beats lexical search because the compiler already knows which hit is the definition and which are call sites or tests — their example is grep for "allocate" in Kubernetes returning 47 unranked matches versus 1 definition plus 12 ranked call sites.

Weigh the source's incentive: Sourcegraph sells code search, and the benchmark is their own. The relative ordering and the order-of-magnitude latency gap are hard to explain away; treat the exact reward deltas as vendor-reported. The 400K figure is a reported inflection point from one benchmark, not a validated constant — use it as the scale at which to start investing in retrieval, not as a cliff.
