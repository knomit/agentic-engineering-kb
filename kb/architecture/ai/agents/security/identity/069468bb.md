---
type: pattern
domain: [agentic-engineering, security, authorization, governance, architecture]
confidence: 0.8
sources: 1
entities: [OWASP, Non-Human Identity, NHI, Agent Identity, Entro Security, OAuth, just-in-time, provenance, attestation]
motifs: [permission-is-not-intent]
refs: ['https://genai.owasp.org/resource/state-of-agentic-ai-security-and-governance/', 'https://genai.owasp.org/download/50592/?tmstv=1754459367', 'https://www.anthropic.com/news/life-sciences-verification-program', 'kb://bc6eac5f37df/kb/architecture/ai/agents/multi-agent/agent-identity-substrate/c87d056d.md', 'kb://bc6eac5f37df/kb/invariants/ai/agents/security/multi-agent/goal-contagion/6cc23314.md', 'kb://bc6eac5f37df/kb/gotchas/ai/agents/multi-agent/dissent-handling/347a5dac.md', 'kb://bc6eac5f37df/kb/conventions/ai/agents/governance/declared-scope-monitoring/519d977b.md']
---
# Non-human identity answers "may this credential connect"; agent identity has to answer "should this reasoner be doing this, right now"

OWASP argues that the industry conflates two distinct layers to its own cost, and that the conflation is why most identity programmes are not agent-ready.

- **Non-Human Identity (NHI)** is the *authentication primitive*: the digital representation and authentication of non-human entities, traditionally static service accounts, API keys, OAuth credentials and certificates. It answers one question, once, at authentication time — "is this entity allowed in?" — and the answer holds until someone rotates the credential.
- **Agent Identity** is the *governance framework* on top: a dynamic cryptographic framework attesting the provenance, intent and authority of entities that reason, plan and execute across trust boundaries.

"NHI tells you a credential is valid. It cannot tell you whether the reasoning entity holding it should be taking the action it is taking right now." The shift is from **verifying the holder at session start to governing the behaviour at the moment of each action** — because an agent reasons, delegates to other agents, and discovers new tools at runtime, so the question becomes "who delegated this agent, what is it trying to do right now, and is it still allowed to do it."

The seven dimensions OWASP contrasts — traditional NHI → agentic identity: nature (static credential → dynamic cryptographic identity); scope (predefined, coarse-grained → intent-based, dynamically discovered, fine-grained); lifespan (long-lived → ephemeral / just-in-time); delegation (basic OAuth scopes → delegation chains with context preservation); auditability (token-based → structured, intent-bound claims); trust model (infrastructure trust → cryptographic attestation plus provenance); risk (over-privileged accounts → trust transitivity and dynamic misuse).

A framework must dynamically enforce three cryptographic assertions: **Provenance** (the agent's integrity), **Attestation** (its identity), and **Intent**.

**WHERE THE THREE ASSERTIONS ACTUALLY STAND, so the taxonomy reads as a gap map rather than a wish list.** Provenance and attestation are shipped: an operator's most-used internal agent scaffold gives every agent a durable identity that is *not tied to the model*, so it survives model upgrades, and binds all of that agent's output to it ([[c87d056d]]). **Intent is the one nobody in this corpus has shipped.** No operator publishes a mechanism that binds an action to a declared, checkable intent at the moment of execution — which is precisely the assertion that would have stopped the failure in [[6cc23314]], where an attributed peer message functioned as authorisation for an action the agent had already reasoned it should not take. Attribution answered "who said this" and the action still went through, because nothing was checking "is this what was authorised."

**THE NEAREST THING TO AN INTENT ASSERTION THE CORPUS HOLDS, AND IT IS INSTRUCTIVELY WEAK.** One operator's verification programme ties an entity's access to use cases it declared in advance, and monitors traffic for activity outside that stated scope ([[519d977b]]). That is intent as a GOVERNANCE object rather than a cryptographic one, and every property differs from what OWASP specifies: the declaration is coarse natural-language text at organisation-or-project granularity, deliberately kept at job-listing specificity so that the holder is willing to write it down at all; it is made once per grant rather than per execution cycle; it is checked AFTER the fact by monitoring for drift rather than at the moment of the action; and no part of it is signed or bound to an individual request. It is worth having here precisely because it shows what survives contact with a real deployment — the part of an intent assertion a holder will actually supply is a SCOPE, not a per-action claim, and the enforcement that fits a scope is detection rather than prevention. The gap above is therefore unclosed, and a reader should not count declared-scope monitoring as the intent assertion arriving.

**AND THERE IS A FOURTH THING NONE OF THE THREE ASSERTIONS COVERS: PRESENCE.** Provenance, attestation and intent are all properties of an agent that acts. None of them is a property of an agent that is *absent*. Where peers control who is on a conversation, an objecting agent can be excluded from it entirely ([[347a5dac]]) — every remaining message perfectly attested, every intent perfectly bound, and the decision record showing consensus. A governance framework built on these three assertions should be read as covering what an agent does, not as covering the composition of the group that decided.

**The concrete implementation trap:** OAuth 2.0 machine-to-machine patterns such as the client-credentials flow use static scopes in reusable tokens, which blocks the real-time revocation agents need. Opaque access tokens fail here for three stated reasons — they are size-limited, static once issued, and lack per-decision authorization visibility — which degrades both fine-grained policy evaluation and post-incident forensics. If your plan is "give the agent a service account and scope it tightly", you have built the NHI layer and none of the agent-identity layer.

**Scale of the existing gap** (Entro Security, 2025 State of NHI): non-human identities outnumber human users roughly 100:1 in most enterprises and up to 500:1 in some; 97% of NHIs carry excessive privileges; 0.01% of machine identities control 80% of cloud resources; 71% are not rotated within recommended timeframes. Vendor-published figures, so treat the exact percentages as indicative — the order of magnitude is the actionable part.

*Source location, because the landing page does not carry the text:* this is mined from the 139-page "State of Agentic AI Security and Governance" report, whose PDF is the second ref. The `/resource/` page is a description with a download button; a reader checking any claim above needs the PDF.
