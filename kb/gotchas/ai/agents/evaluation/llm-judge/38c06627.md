---
type: pattern
domain: [agentic-engineering, evaluation, llm-judge]
confidence: 0.8
sources: 2
entities: [applied-llms, LLM-as-judge, Goodhart's Law]
refs: ['https://applied-llms.org/', 'https://eugeneyan.com/writing/llm-patterns/']
---
# LLM judges have position, length and self-preference biases you must actively cancel

When scoring with an LLM judge, prefer pairwise comparison of two outputs over asking for a single-output Likert score — relative judgements are far more stable than absolute ones. But pairwise introduces its own artefacts that will silently inflate your numbers unless you cancel them:

- Position bias: the judge favours one slot regardless of content. Cancel it by running each pair twice with the order reversed and only counting consistent verdicts.
- Length bias: judges prefer longer responses. Control for it explicitly, or you will "improve" your system by making it more verbose.
- Self-preference bias: a judge scores its own family's outputs higher. Measured self-preference — Claude-v1 showed a 25% higher win rate for its own outputs, GPT-4 about 10%. The operational consequence is specific: never let the model you are shipping also be the judge that decides it improved, and be suspicious of any A/B where the judge shares a family with one arm.
- Allow ties. Forcing a winner injects noise on genuinely equivalent pairs.
- Ask for chain-of-thought reasoning before the verdict, not after.

On how good a judge can get: under single-answer grading GPT-4 reached 85% agreement with human raters, against 81% agreement between the humans themselves — i.e. a well-built judge can sit at roughly the human-to-human ceiling, which is the honest bar to compare against rather than 100%. Separately reported correlations are more sobering: 0.514 Spearman with human judgement on news summarization, and 0.55 Spearman on Vicuna-benchmark model rankings.

The applied-llms authors report taking judge-to-human agreement from 68% to 94% over three iterations of this loop — the useful implication being that an unrefined judge sitting at ~68% agreement looks perfectly plausible in a dashboard while being wrong about a third of the time.

Related trap from the same source: over-optimising a single metric degrades others. Focusing on needle-in-a-haystack recall measurably reduced extraction and summarization performance. Evaluate on realistic tasks, not synthetic recall probes.

Caveat on the numbers: the self-preference and agreement figures come from 2023-era evaluations (Claude-v1, GPT-4) and the exact magnitudes should not be assumed to carry over to current models. The direction of the bias is the durable part; re-measure the size on whatever judge you actually deploy.
