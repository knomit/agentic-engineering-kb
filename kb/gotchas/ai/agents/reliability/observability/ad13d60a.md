---
type: observation
domain: [agentic-engineering, reliability, operations, observability, distributed-systems]
confidence: 0.85
sources: 1
entities: [Amazon, AWS Builders' Library, load shedding, brownout, goodput, median latency]
refs: ['https://builder.aws.com/content/3Eun1EEyX6p2e3VYNyRLSJzLuMV/using-load-shedding-to-avoid-overload']
---
# Shed and fast-failed requests drag your latency metrics down, so an overloaded agent service looks fastest at its worst moment

Rejecting a request is far cheaper than serving one, so every shed request enters your latency histogram as a very fast sample. Amazon's rule: if a service is shedding 60% of its traffic, its median latency can look excellent while the latency of the requests it actually served is terrible — the successful-request latency is being diluted by the fast failures.

This is sharp for agent systems because the cheap-rejection path is everywhere: a tool server returning a 429, an orchestrator refusing to spawn a subagent past a concurrency cap, a router rejecting a request before it reaches the model. All of them complete in milliseconds against multi-second real work, so the more overloaded the system gets, the better its aggregate latency graph looks.

What to do: emit shed/rejected requests on a separate metric stream and compute latency percentiles over accepted requests only. Keep the rejection count and rejection rate as first-class metrics rather than letting rejections hide inside a p50.

This does NOT mean stop measuring rejected requests — it means stop mixing them into the same distribution as served requests. Amazon also alarms on the rejection RATE specifically, and treats a persistently non-zero false-positive rate (rejecting while capacity was actually available) as evidence the service is either tuned too sensitively or suffering a load-balancing imbalance, not as normal operation.
