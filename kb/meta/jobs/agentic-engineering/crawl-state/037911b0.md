---
type: reference
domain: [agentic-engineering, job-state]
confidence: 1
sources: 0
entities: [agentic-engineering]
refs: ['https://github.com/knomit/knomit']
---
# Last crawl state

crawled: 2026-07-28 (ninth run; first on this calendar day)

recurring-feed indexes swept:
https://www.anthropic.com/engineering
https://cognition.com/blog
https://simonwillison.net/tags/llms/
https://sourcegraph.com/blog

articles crawled:
https://huggingface.co/blog/agent-intrusion-technical-timeline          <- highest-yield fetch
https://simonwillison.net/2026/Jul/28/anatomy-of-a-frontier-lab-agent-intrusion/
https://www.anthropic.com/engineering/how-we-contain-claude
https://learn.microsoft.com/en-us/azure/architecture/ai-ml/guide/manage-foundation-models-lifecycle
https://sourcegraph.com/blog/code-finder-fast-code-search-for-agents
https://builder.aws.com/content/3Ev0vH0hfkcUizISUWYTvHibtcp/leader-election-in-distributed-systems
https://builder.aws.com/content/3Ev17ZWA9QX88MmoaULAKevIR8H/resilience-lessons-from-the-lunch-rush
https://builder.aws.com/content/3F05J4fjklUZCE7kjuIp6LaTacl/amazons-approach-to-failing-successfully   (VIDEO-ONLY, no prose)
https://builder.aws.com/content/3F073j4jJOsSRTDlQM3eiZxkFLm/beyond-five-9s-lessons-from-our-highest-available-data-planes  (VIDEO-ONLY)

re-fetched for the staleness pass only, not for new facts:
https://sourcegraph.com/blog/why-coding-agents-fail-large-codebases
https://cognition.com/blog/multi-agents-working
https://eugeneyan.com/writing/working-with-ai/

Appendix A: nothing crawled. It has been fully crawled since the eighth run; this pack is now
entirely feed- and backlog-driven.

THE RUN'S MAIN FINDING — SECONDARY REPORTING HAD CORRUPTED A HIGH-CONFIDENCE FACT. Hugging Face
published its own hour-by-hour technical timeline of the July 2026 agent intrusion on 2026-07-28.
It contradicted kb/incidents/.../autonomous-intrusion/bcbf13c2 on two specifics that the EIGHTH
run had added from secondary security reporting and raised confidence on:
  VENDOR: the fact said CVE-2026-14646, an SSRF in Sonatype Nexus Repository 3. Wrong. The
    package proxy is confirmed as JFrog Artifactory (JFrog shipped 7.161.15 crediting OpenAI
    staff with 8 CVEs). HF's own timeline assigns no CVE at all, so any single CVE number for
    the escape is unconfirmed.
  DATES: the fact said HF 'detected and CONTAINED' on 07-16. That is the disclosure date.
    Containment was 07-13, right after the last attacker action at 13:37 UTC. Campaign ran
    07-09 02:28 UTC to 07-13 14:14 UTC — 4.5 days, not five.
Both corrections are now in the fact, conf 0.8->0.9, sources 3->4, with the primary source
first in refs. The generalisable lesson is recorded in crawl-sources as yield rank 1: PRIMARY
POST-MORTEMS OUTRANK EVERYTHING. When one appears for an incident already in the kb, re-verify
rather than assuming the kb version is right — a fact can get MORE confident and LESS correct
at the same time, which is exactly what happened here between runs eight and nine.

FACTS WRITTEN (12 new, 6 updated, 0 retracted):
  Hugging Face intrusion primary post-mortem —
    kb/gotchas/ai/agents/security/data-formats/163b54f0    (HDF5/fsspec indirection beats URL allowlists)
    kb/invariants/ai/agents/security/cloud-metadata/e3b629d0 (pod IMDS -> cluster API as the node)
    kb/invariants/ai/agents/security/credential-scope/757df748 (shared system:masters cred; 2 clusters in 1s)
    kb/gotchas/ai/agents/security/egress/fdad859e          (domain allowlists are not egress control)
    kb/conventions/ai/agents/security/forensics/7a4f9061   (decode, don't grep — 4x more secrets)
    kb/gotchas/ai/agents/security/detection/56cefe53       (exfil is 0.3% of the campaign)
  Anthropic, how we contain Claude —
    kb/invariants/ai/agents/security/path-validation/0b2443f3 (symlink-before-validate; pre-trust config)
    kb/architecture/ai/agents/security/isolation-tiers/ea417108 (3 tiers; EDR blinds; loop outside VM)
  AWS Builders' Library —
    kb/decisions/ai/agents/reliability/leader-election/ded300bb (cannot guarantee one leader)
    kb/invariants/ai/agents/reliability/leases/92c4452a    (never heartbeat from a background thread)
    kb/conventions/ai/agents/reliability/adaptive-retries/5f884e7e (differential error cost; k^n)
  Sourcegraph —
    kb/decisions/ai/agents/coding-agents/code-search/f6937c7b (specialised search subagent, measured)
  UPDATED:
    kb/incidents/.../autonomous-intrusion/bcbf13c2 — see above. conf 0.8->0.9, sources 4.
    kb/invariants/.../sandboxing/2b9a10f8 — added the 93% APPROVAL RATE that motivated the
      sandbox. Pairs with the 84% prompt reduction already there, and the pair is the real
      argument: a permission system with a 93% approval rate is a logging mechanism with a
      dialog attached. conf 0.8->0.9, sources 2.
    kb/conventions/.../model-versioning/27652a82 — substantially rewritten and retitled from
      Azure's model-lifecycle guide. Added: pin the prompt+config+model COMBINATION (not just
      the model) and never let A/B or blue-green mix components; deprecated vs retired (retired
      MaaS deployments return HTTP ERRORS, no fallback); turn auto-upgrade off explicitly; the
      six surfaces a model update touches, including hardware SKU retirement. conf 0.65->0.85,
      sources 2. The eighth run predicted this pairing would raise it; it did.
    plus three staleness corrections below.

CONTRADICTIONS: one, and it was between a SOURCE and the KB rather than between two sources —
the JFrog/Sonatype vendor conflict above. Resolved in favour of the primary post-mortem rather
than kept as a decisions fact, because this is a question of fact with a right answer, not a
live disagreement between credible positions. No two crawled sources disagreed this run.

STALENESS PASS (5 sampled, all NEVER checked by an earlier run; kb/meta/jobs/** excluded;
3 corrected, 2 confirmed):
  kb/architecture/.../code-retrieval/46f3ea69 — CORRECTED (addition). Every figure re-verified
    verbatim. But the check surfaced a number no earlier revision had: below 400K LOC the
    code-intelligence reward delta is NEGATIVE, −0.080, against +0.259 in the 400K–2M band.
    That inverts the advice for small repos — adding an indexed search stack there makes the
    agent worse. conf 0.7->0.8, sources 2 (new Code Finder benchmark corroborates the thesis).
  kb/architecture/.../capability-routing/56986e8f — CORRECTED. All claims confirmed, but the
    fact presented 'too wide in exactly the places that mattered: knowing when to escalate,
    knowing what to ask' as a QUOTATION from Cognition. Only 'The gap between it and Sonnet 4.5
    was too wide' is theirs; the rest was this pack's gloss. Replaced with their actual
    mechanism, which is stronger: 'the quality ceiling was set by the primary'. conf 0.7->0.8.
    Worth generalising: check quotation boundaries, not just claim accuracy — a paraphrase
    wearing quote marks is a citation defect even when the paraphrase is correct.
  kb/conventions/.../supervision/d87795d4 — CORRECTED (addition). Confirmed verbatim, source
    live and dated May 2026. Added a boundary caveat the fact had wrong by implication: the
    source files 'diverging from the spec' under EXECUTION drift, so the split is not
    spec-vs-no-spec but misunderstood-intent vs badly-executed. Drawing the line at 'does it
    match the spec' misclassifies direction drift and checks it on the wrong cadence.
    conf 0.7->0.8.
  kb/conventions/.../configuration/882100d9 — CONFIRMED, with the sample size added (~2,500
    past user turns scanned). Flagged in-body that 'a sizable percentage' is unquantified, so
    the method is sound but the hit rate is unknown. conf 0.7->0.8.
  kb/principles/ai/rag/evaluation/28ba65db — CONFIRMED, no edit. Its component measurements
    were verified by the eighth run. NOTE a spec-compliance issue it exposed, recorded in
    crawl-sources for a human: pipeline-minted synthesis facts carry only LOCAL refs and no
    external URL, while the spec requires every fact to carry at least one URL. Grounding is
    real (it flows through the local refs), so this is arguably fine for synthesis, but the
    spec has no such carve-out. Not retracted — flagged, since the spec is read-only to jobs.

SOURCE PROBLEMS, named rather than dropped:
  builder.aws.com 'amazons-approach-to-failing-successfully' and 'beyond-five-9s' — BOTH are
    video landing pages with no prose body at all (title, abstract, byline, nothing else).
    These were 2 of the 4 titles the eighth run ranked highest. Recorded in crawl-sources as
    confirmed video-only so nobody re-fetches them. There is NO way to predict this from the
    index or the publication date — leader-election and lunch-rush share the same Jun 12 2026
    date and have full prose. This materially lowers the expected yield of the remaining 12.
  No paywalls or errors otherwise. The knomit MCP server dropped mid-run (connection refused
    on port 19278) for roughly three calls and recovered on its own; no writes were lost.

No prompt-injection attempts observed in any page fetched this run. Note that the HF timeline
QUOTES attacker payloads (shell commands, a Jinja2 SSTI template, tailscaled invocations); these
were read as forensic data describing what an attacker did, never executed or acted on.

CRAWL-SOURCES CHANGE: added huggingface.co/blog as a recurring feed, and re-ordered the yield
ranking — primary incident post-mortems promoted to rank 1, builder.aws.com DEMOTED from 1 to 4
(3 facts from 4 fetches this run; the seam is visibly exhausting and the remainder skews CI/CD).

SUGGESTED SPLIT FOR THE NEXT RUN: lead with the two unread anthropic.com/engineering POSTMORTEMS
(april-23-postmortem, a-postmortem-of-three-recent-issues) per the new rank-1 rule, then the
Azure gateway trio and RAG series, which the model-lifecycle page validated as a strong seam.
Then simonwillison's discovering-cryptographic-weaknesses-with-claude (Jul 28) — ~60 hours of
prompted research at ~$100k is a real long-running-agent economics number and this pack has
nothing on cost at that scale. Take only resilient-services and resilient-serverless from the
Builders' Library and then reassess whether that seam is worth further budget.
