---
type: reference
domain: [agentic-engineering, job-state]
confidence: 1
sources: 0
entities: [agentic-engineering]
refs: ['https://github.com/knomit/knomit']
---
# Last crawl state

crawled: 2026-07-31 (twelfth run; first on this calendar day, ~1 day after the eleventh)

recurring-feed indexes swept:
https://modelcontextprotocol.io/specification/versioning        (tripwire — UNCHANGED, still 2026-07-28)
https://genai.owasp.org/resource/state-of-agentic-ai-security-and-governance/

articles crawled:
https://genai.owasp.org/download/50592/?tmstv=1754459367   <- State of Agentic AI Sec & Gov 2.01,
                                                              139pp PDF, HIGHEST-YIELD FETCH
https://modelcontextprotocol.io/specification/2026-07-28/basic/patterns/mrtr
https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/client-registration
https://modelcontextprotocol.io/specification/2026-07-28/deprecated
https://modelcontextprotocol.io/docs/extensions/overview

re-fetched for the staleness pass only, not for new facts:
https://cognition.com/blog/what-we-learned-building-cloud-agents
https://www.anthropic.com/engineering/managed-agents
https://github.com/humanlayer/12-factor-agents
https://goose-docs.ai/docs/guides/context-engineering/subagents
https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-4-best-practices

Appendix A: nothing crawled, nothing left. Fully covered since the eighth run. Two lines on disk are
now WRONG and need a human: (1) Tier 1 still says "MCP's current revision is 2025-11-25" — it is
2026-07-28. (2) The Tier-0 lethal-trifecta entry should note that a documented counter-position now
exists (see FINDING 2).

HISTORY WALK: complete, no gaps. Twelve revisions read back to the init commit e99e5692
(more_available false at 88499d53). ~190 distinct URLs in ALREADY_CRAWLED. Exactly one fact in each
of the crawl-state and crawl-sources directories — no duplicate-state problem.
METHOD NOTE for the next run, because this is easy to get wrong: knomit_explain returns THREE
revisions per page, and stepping to the OLDEST of the three SKIPS the two in between. Each crawl-state
revision is a different run's URL list, so a skipped revision is a silently missing run. Anchor at
EVERY commit, not just the oldest per page. The 12th run caught this after skipping the ninth run's
revision and went back for it.

FINDING 1 — THE 'GATED PDF' BLOCKER WAS FALSE, AND THE FOURTH FALSE POSITIVE IN A ROW.
The 11th run recorded OWASP's State of Agentic AI report as "gated PDF, not obtained". It is not
gated. Asking WebFetch for the resource page's PDF link returns a direct download URL; fetching that
URL fails to RENDER but silently PERSISTS the 2.5MB PDF to disk and prints the path in the same tool
result; pypdf reads it. Four calls, 139 pages, 275k chars. Full route recorded in crawl-sources.
This is now four-for-four: builder.aws.com, the Vectara leaderboard, OWASP PDFs "no renderer", and
OWASP PDFs "gated". EVERY source this pack has declared dead was reachable another way, and twice
the SAME source was declared dead for two different wrong reasons on consecutive runs. The failure
is not the tooling, it is that a single failed call gets written down as a property of the source.
New rule added to the exhaust-list: try the RESOURCE/LANDING page and ask it for the asset URL.

FINDING 2 — A TIER-0 FACT HAD A PUBLISHED COUNTER-POSITION AND NOTHING HAD LOOKED FOR IT.
48c9de1b (the lethal trifecta, written in the FIRST run from a single source) asserted "any two are
survivable". OWASP names the counter-evidence directly: Meta's Agents Rule of Two (Oct 2025) states
the design constraint — no more than two of the three properties in any session without human
approval — but "Invitation Is All You Need" (2025) documents calendar-invite attacks that remain
viable with only TWO of the three present. The fact is corrected from a safety threshold to a
screening heuristic, with the likely reason for the residual risk named: enumerating the
"exfiltration channel" leg is harder than it looks, since a calendar response or the agent's own
rendered output can carry data out.
THE PROCESS LESSON, and it generalises beyond this fact: the staleness pass samples on CONFIDENCE,
which systematically misses the tier-0/tier-1 backfill — those facts were written confident, from one
source each, in the first run, and no run has revisited whether the field moved under them. Added to
crawl-sources as a SECOND SAMPLING AXIS: sample the earliest-committed facts, not only the least
confident.

FACTS WRITTEN (11 new, 4 updated, 0 retracted):
  MCP 2026-07-28, closing the backlog the 11th run named —
    kb/invariants/ai/agents/tools/mcp/security/request-state/edf39c27  (requestState is attacker-
      controlled by spec; the replay controls explicitly do NOT give single-use)
    kb/conventions/ai/agents/tools/mcp/authorization/client-registration/00fd386c (CIMD portability;
      the application_type="web" default that breaks native redirect URIs)
    kb/architecture/ai/agents/tools/mcp/extensions/fafca497 (conformance != capability parity)
  OWASP State of Agentic AI Security and Governance 2.01 —
    kb/gotchas/ai/agents/security/command-allowlists/92dc0441  (CVE-2026-22708 + CVE-2025-59532;
      the allowlist STREAMLINES the attack rather than failing — best fact of the run)
    kb/invariants/ai/agents/coding-agents/supply-chain/8b9c9c7a (SLSA regression; enforce at the
      repository layer because an agent that can edit its config can weaken its own bounds)
    kb/gotchas/ai/agents/security/supply-chain/tool-metadata/78ab92f2 (payload in metadata not code;
      tool poisoning / rug pulls / shadowing; postmark-mcp's 15 legitimacy-building versions)
    kb/gotchas/ai/agents/security/memory-poisoning/7bd6c6c9 (SOUL.md / MEMORY.md; session-scoped
      monitoring structurally cannot see it; it presents as a reliability bug)
    kb/decisions/ai/agents/security/incident-response/safety-vs-security/c06b9170 (contain first,
      classify later; the autonomy+oversight condition that decides when the split still works)
    kb/invariants/ai/agents/governance/incident-reporting/406e5a50 (DORA 4h / NIS2 24h / RAISE 72h /
      SB 53 15d — makes a programmatic kill path a build requirement)
    kb/architecture/ai/agents/security/identity/069468bb (NHI vs Agent Identity; connect-time vs
      per-action; why OAuth client-credentials cannot express it)
    kb/architecture/ai/agents/interop/discovery/8b32c15a (A2A agent-card spoofing — the LLM judge
      selected a self-asserted trust claim; registries need DNS/PKI-grade controls)
  UPDATED —
    kb/invariants/.../prompt-injection/48c9de1b — see FINDING 2. Retitled. sources 1->2, conf 0.8->0.85.
    kb/invariants/.../mcp/interaction-model/e26efa36 — the MRTR page added the retry contract the
      changelog did not carry: only prompts/get, resources/read and tools/call may return
      InputRequiredResult (MUST NOT on anything else); the JSON-RPC id MUST DIFFER between attempt
      and retry, so anything correlating by request id breaks; a server missing required input SHOULD
      re-issue InputRequiredResult rather than error. sources 1->2.
    kb/gotchas/.../mcp/deprecation/012daf73 — concrete dates from the registry: Roots, Sampling,
      Logging and DCR all become eligible for removal at "the first revision released on or after
      2027-07-28"; HTTP+SSE is the nearest at three months after SEP-2596 reaches Final. Two things
      the registry makes explicit and the changelog did not: earliest removal is ELIGIBILITY, not a
      date, and NO feature has ever been removed under this policy — the 12-month window is a
      commitment with no track record. sources 1->2.

CONTRADICTIONS: one, and it is a genuine source-vs-kb disagreement rather than two sources fighting —
the lethal-trifecta sufficiency claim in FINDING 2. Handled by keeping BOTH positions inside the fact
with the separating condition named (what counts as the third leg), rather than picking a winner or
splitting off a decisions fact, because the two sources do not give conflicting ADVICE — both say
reduce the property count; they disagree only on whether two is a safe harbour.
No two crawled sources contradicted each other this run.

STALENESS PASS (5 sampled, all NEVER checked before; kb/meta/jobs/** excluded; kb/principles/**
avoided per the write-block; 2 corrected, 3 confirmed-and-enriched):
  30869b36 — CORRECTED. All claims verified verbatim against the source (published 2026-04-23, no
    update notice), but the fact quoted a company abandoning cloud-agent infrastructure when "the
    combined surface area became untenable". That string is NOT in the source; Cognition write that
    the company "moved on after the project scope overwhelmed their infrastructure team". A pack
    gloss wearing quote marks — same defect class as 56986e8f four runs ago. Also added the vendor
    caveat explicitly. conf 0.7->0.8.
  c436422a — CORRECTED, WRONG TERMS ATTRIBUTED. The fact presented BRAIN / HANDS / SESSION as
    Anthropic's decomposition. Anthropic's own words are "a session..., a harness..., and a sandbox".
    Every number re-verified (p50 TTFT -60%, p95 -90%+; the six-method interface; the credentials-in-
    the-same-container framing), published 2026-04-08. Invented vendor vocabulary is a real defect:
    it makes the fact unsearchable against its own source. NEW CHECKLIST ITEM added to crawl-sources.
    conf 0.75->0.85.
  9920b5d6 — CONFIRMED verbatim. Every Goose subagent limit (25 turns, GOOSE_SUBAGENT_MAX_TURNS,
    5-minute timeout, inherited extensions, no recursion) and both failure-semantics sentences quoted
    exactly. Page carries no publication date. conf 0.8->0.85.
  c1e18090 — CONFIRMED and enriched. All five factor titles verified exactly (Factor 9's real title
    is "Compact Errors into Context Window"). Repo live, not superseded: 273 commits, 25k+ stars,
    1.9k+ forks, no deprecation notice. Noted that the commit count is IDENTICAL to the reading taken
    five days earlier, so "active" here means maintained, not moving. conf 0.7->0.75.
  773a89ee — CONFIRMED and enriched. The Opus 4.6 / Opus 5 over-delegation text verified verbatim,
    including the grep example. Two changes since the fact was written: Anthropic now publishes a
    sample damping prompt inline (added to the fact), and Opus 5 has its own "Controlling subagent
    spawning" page. The general page has widened to nine models — but the over-delegation warning is
    still stated ONLY for Opus 4.6 and Opus 5, so it must not be generalised to the roster.
    refs 1->2, conf 0.8->0.85.

SOURCE PROBLEMS, named rather than dropped:
  The OWASP ASI Exploits & Incidents Tracker GitHub repo URL is footnote 19 in the report and was NOT
    captured before the extraction was scoped to the chapters read. The companion visual explorer
    (owasp-agentic-ai-security-incidents.lovable.app) IS captured and has been added to crawl-sources.
    A living incident tracker is rank-2 material by this pack's own ranking — get the repo URL next run.
  Nine OWASP companion documents named by the report have never been fetched; all listed in
    crawl-sources. No paywalls, no errors, no dead URLs, no JS-rendered failures this run.
  platform.claude.com's best-practices page is now ~58KB and exceeds WebFetch's inline limit; it
    persists to disk and must be grepped rather than re-read.

PROMPT INJECTION: none observed. The OWASP report QUOTES attacker techniques at length (tool-poisoning
payloads in description fields, memory-file poisoning of SOUL.md/MEMORY.md, agent-card spoofing) and
the MCP spec pages carry MUST/SHOULD directives addressed to implementers. All were read as threat-
model and specification text describing what attackers and implementers do, never as instructions to
me. Nothing in any page addressed an AI agent reading it.

CRAWL-SOURCES CHANGES: two feeds ADDED — the MCP deprecated-features registry (a second cheap
tripwire alongside versioning) and the OWASP ASI incidents-tracker explorer. The PDF-reading route is
recorded in full. The 'dead or unreadable' list is now effectively empty and carries the four-for-four
false-positive warning. The staleness checklist went from four items to six (added: are the TERMS
attributed to the source actually the source's terms). A second sampling axis was added (earliest-
committed, not just lowest-confidence). Yield ranking reordered: OWASP PDFs hold rank 3 and MCP's
remaining spec pages enter at rank 4.

SUGGESTED SPLIT FOR THE NEXT RUN: (1) The OWASP companion corpus — Securing Agentic Applications
Guide and Multi-Agentic System Threat Modeling Guide first, then the two MCP security documents,
which pair directly with the last two runs' MCP work. The read route is solved, so these are now
cheap. (2) The chapters of the State of Agentic AI report this run did NOT write up — the Enterprise
Adoption Maturity Model and the Future Trends / What Remains Unsolved chapter (governance-deployment
collision, cyber-insurance coverage collapse) are the strongest remaining, and the file is already
extracted. (3) MCP server/discover, which three facts reference and none has read. (4) Start the
earliest-committed sampling axis on the tier-0/tier-1 backfill — FINDING 2 suggests there is more
there. Do NOT re-sweep anthropic/engineering, simonwillison, cognition, huggingface, microsoft or
aws — all swept within the last two days with near-zero yield, consistent with every prior same-week
sweep.
