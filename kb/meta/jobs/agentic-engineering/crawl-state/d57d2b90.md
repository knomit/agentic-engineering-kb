---
type: reference
domain: [agentic-engineering, job-state]
confidence: 1
sources: 0
entities: [agentic-engineering]
refs: ['https://github.com/knomit/knomit']
---
# Last crawl state

crawled: 2026-07-31 (twelfth run; first on this calendar day, ~1 day after the eleventh)

*** READ THIS FIRST — THE REVISION HISTORY OF THIS FACT WAS BROKEN ON 2026-07-31 ***
This fact was DELETED and recreated during the twelfth run. The original file was
kb/meta/jobs/agentic-engineering/crawl-state/037911b0.md, carrying twelve revisions back to the init
commit e99e5692. A knomit_review session (run at the user's request, scoped to
domain=agentic-engineering) performed an AUTOMATIC dedup merge at commit e04282e1 with the message
"dedup: merge .../crawl-state/037911b0.md into .../crawl-sources/fa385bda.md [review]". The merge was
NOT requested in any response I submitted — I never put crawl-state in a merges array — and it did
not copy crawl-state's body into crawl-sources; it deleted the fact and left a dangling local ref.
CONSEQUENCES FOR THE HISTORY WALK, and they are the important part:
  * THIS FACT'S HISTORY NOW STARTS AT THE TWELFTH RUN. Walking it backwards will terminate here.
  * Runs 1-11 are NOT lost — they are in git under the OLD path 037911b0.md, reachable via
    knomit_explain with that file path and an explicit `commit`. The chain of commits, newest first:
    d2006865 e780b5c4 260e885d 48f3f4e0 ba492760 9b0c45d4 37d4b6ed f673d976 7dc18043 57589a59
    88499d53 e9152ff3, with the init commit e99e5692 beneath them. ~190 distinct URLs total.
  * A future run that walks only THIS fact will believe the pack has one run of history and may
    re-crawl everything. Walk the commit list above as well until a human repairs the lineage.
*** THE OPERATIONAL LESSON, which is the most valuable thing this run produced ***
DO NOT RUN knomit_review SCOPED TO domain=agentic-engineering. The job-state facts (crawl-state,
crawl-sources) and the SPEC MIRROR all carry that domain tag, so they enter the review's clustering
and become eligible for automatic dedup/merge. crawl-state and crawl-sources are textually similar
(both are long URL-and-notes documents about the same sources), which is exactly what a dedup pass
latches onto. The pack spec excludes kb/meta/jobs/** from the STALENESS pass but says nothing about
knomit_review, and the review tool does not exclude it on its own.
IF A REVIEW IS RUN AT ALL, scope it to exclude job state, verify nothing under kb/meta/jobs/ appears
in any work item, and re-query kb/meta/jobs/agentic-engineering/ afterwards to confirm both facts
still exist. Answering "keep" for a job-state fact inside an item is NOT sufficient protection — the
merge here happened outside any item I answered.

recurring-feed indexes swept:
https://modelcontextprotocol.io/specification/versioning        (tripwire — UNCHANGED, still 2026-07-28)
https://genai.owasp.org/resource/state-of-agentic-ai-security-and-governance/

articles crawled:
https://genai.owasp.org/download/50592/?tmstv=1754459367   <- State of Agentic AI Sec & Gov 2.01,
                                                              139pp PDF, HIGHEST-YIELD FETCH
https://modelcontextprotocol.io/specification/2026-07-28/basic/patterns/mrtr
https://modelcontextprotocol.io/specification/2026-07-28/basic/authorization/client-registration
https://modelcontextprotocol.io/specification/2026-07-28/deprecated
https://modelcontextprotocol.io/docs/extensions/overview

re-fetched for the staleness pass only, not for new facts:
https://cognition.com/blog/what-we-learned-building-cloud-agents
https://www.anthropic.com/engineering/managed-agents
https://github.com/humanlayer/12-factor-agents
https://goose-docs.ai/docs/guides/context-engineering/subagents
https://platform.claude.com/docs/en/build-with-claude/prompt-engineering/claude-4-best-practices

Appendix A: nothing crawled, nothing left. Fully covered since the eighth run. Two lines on disk are
now WRONG and need a human: (1) Tier 1 still says "MCP's current revision is 2025-11-25" — it is
2026-07-28. (2) The Tier-0 lethal-trifecta entry should note a documented counter-position now exists.

HISTORY WALK (performed BEFORE the deletion, and it completed): twelve revisions read back to the
init commit e99e5692, more_available false at 88499d53. ~190 distinct URLs in ALREADY_CRAWLED.
Exactly one fact in each of the crawl-state and crawl-sources directories at that time.
METHOD NOTE, easy to get wrong: knomit_explain returns THREE revisions per page, and stepping to the
OLDEST of the three SKIPS the two in between. Each crawl-state revision is a different run's URL
list, so a skipped revision is a silently missing run. Anchor at EVERY commit, not just the oldest
per page. This run caught it after skipping the ninth run's revision and went back for it.

FINDING 1 — THE 'GATED PDF' BLOCKER WAS FALSE, AND THE FOURTH FALSE POSITIVE IN A ROW.
The 11th run recorded OWASP's State of Agentic AI report as "gated PDF, not obtained". It is not
gated. Asking WebFetch for the resource page's PDF link returns a direct download URL; fetching that
URL fails to RENDER but silently PERSISTS the 2.5MB PDF to disk and prints the path in the same tool
result; pypdf reads it. Four calls, 139 pages, 275k chars. Full route recorded in crawl-sources.
Four-for-four now: builder.aws.com, the Vectara leaderboard, OWASP PDFs "no renderer", OWASP PDFs
"gated". Twice the SAME source was declared dead for two different wrong reasons on consecutive runs.
The failure is not the tooling — it is that a single failed call gets written down as a property of
the source. New rule: try the RESOURCE/LANDING page and ask it for the asset URL.

FINDING 2 — A TIER-0 FACT HAD A PUBLISHED COUNTER-POSITION AND NOTHING HAD LOOKED FOR IT.
48c9de1b (lethal trifecta, written in the FIRST run from a single source) asserted "any two are
survivable". OWASP names the counter-evidence: Meta's Agents Rule of Two (Oct 2025) gives the design
constraint — no more than two properties without human approval — but "Invitation Is All You Need"
(2025) documents calendar-invite attacks viable with only TWO of the three. Corrected from a safety
threshold to a screening heuristic, with the likely reason named: enumerating the "exfiltration
channel" leg is harder than it looks, since a calendar response or the agent's own rendered output
can carry data out.
PROCESS LESSON: the staleness pass samples on CONFIDENCE, which systematically misses the tier-0/
tier-1 backfill — written confident, one source each, first run, never revisited. Added to
crawl-sources as a SECOND SAMPLING AXIS: sample the earliest-committed facts too.

FACTS WRITTEN THIS RUN (11 new from crawling, 4 updated; plus 3 from the review, plus 2 repairs):
  MCP 2026-07-28 — edf39c27 (requestState is attacker-controlled by spec; the replay controls
    explicitly do NOT give single-use), 00fd386c (CIMD portability; application_type="web" default
    breaks native redirect URIs), fafca497 (conformance != capability parity)
  OWASP State of Agentic AI 2.01 — 92dc0441 (CVE-2026-22708 + CVE-2025-59532; the allowlist
    STREAMLINES the attack rather than failing — best fact of the run), 8b9c9c7a (SLSA regression;
    enforce at the repository layer), 78ab92f2 (payload in metadata not code; tool poisoning, rug
    pulls, shadowing; postmark-mcp's 15 legitimacy-building versions), 7bd6c6c9 (SOUL.md/MEMORY.md;
    session-scoped monitoring structurally cannot see it), c06b9170 (contain first, classify later),
    406e5a50 (DORA 4h / NIS2 24h / RAISE 72h / SB 53 15d), 069468bb (NHI vs Agent Identity),
    8b32c15a (A2A agent-card spoofing; the LLM judge selected a self-asserted trust claim)
  UPDATED — 48c9de1b (see FINDING 2; sources 1->2, conf 0.85), e26efa36 (MRTR retry contract: only
    prompts/get, resources/read, tools/call may return InputRequiredResult; the JSON-RPC id MUST
    DIFFER between attempt and retry), 012daf73 (registry dates: eligible for removal at "the first
    revision released on or after 2027-07-28"; earliest removal is ELIGIBILITY not a date; NO feature
    has ever been removed under this policy)

STALENESS PASS (5 sampled, all never checked before; 2 corrected, 3 confirmed-and-enriched):
  30869b36 CORRECTED — quoted "the combined surface area became untenable"; not in the source, which
    says a company "moved on after the project scope overwhelmed their infrastructure team".
    conf 0.7->0.8.
  c436422a CORRECTED — presented BRAIN/HANDS/SESSION as Anthropic's decomposition; their words are
    "a session..., a harness..., and a sandbox". conf 0.75->0.85.
  9920b5d6 CONFIRMED verbatim (conf 0.85). c1e18090 CONFIRMED + enriched (conf 0.75).
  773a89ee CONFIRMED + enriched: Opus 5 now has its own "Controlling subagent spawning" page; the
    general page widened to nine models but the warning is still stated ONLY for Opus 4.6 and Opus 5.

*** KNOMIT_REVIEW PASS (user-requested, after the crawl) ***
37 of 48 work items completed and applied before I stopped it; ~180 facts read across 20 prune and
17 distill clusters.
PRUNE: 0 retractions, 0 merges, 0 confidence changes — a real signal, not a null one. It says the
spec's query-before-write rule is being followed, so there are no duplicates to collapse. The tool
clusters by TOPICAL SIMILARITY, not redundancy, so a big cluster is not evidence of duplication.
DISTILL: 3 new synthesis facts, 14 clusters declined as already-covered (in most, the existing
synthesis was itself an input — a0cc9098, 1feacc9e, 2c0e8e7c, 6363ce1b, 6bc7bda3, 9c9d9bd1, 99d75036,
0c0d403f, 984a2491, f5bf2d02, f18eceb3, 00d3ef19, b9b45ff5, f6d06869).
  kb/invariants/ai/agents/reliability/mode-transitions — a MODE is a branch whose condition is "under
    stress", making it both the least-tested code and the code most certain to run fleet-wide at once.
  kb/invariants/ai/agents/reliability/independence-assumptions — independence is assumed in three
    places (replicas, clients, successive calls in one operation) and false in all three.
  kb/conventions/ai/agents/operations/observability/agent-telemetry-inversions — the unit is a RUN
    not a request; the constraint is BYTES PER ENTRY not entries/sec; the content is adversarial AND
    read back by models.

REVIEW FINDING — CORRECTIONS DO NOT PROPAGATE FROM SOURCE FACTS TO THE SYNTHESES BUILT ON THEM.
New defect class, and the worst instance is unfixable by a job:
  * 00d3ef19 (kb/principles/ai/agents/tools) STILL carries, inside quote marks, the exact pseudo-quote
    the 11th run corrected in its source 96ebc34e: "More tools sometimes made performance worse due to
    poor strategic usage". Sourcegraph never wrote that. UNFIXABLE FROM A JOB — kb/principles/ is
    read-only to jobs. FOR A HUMAN.
  * b9b11a69 (kb/decisions) had inherited the "combined surface area" paraphrase from 30869b36.
    Job-writable, and CORRECTED this run along with the brain/hands shorthand it also inherited.
  * 984a2491 shows the correct behaviour and proves it is achievable — it carries an explicit
    "CITATION FIX 2026-07-28" note because that run chased its correction into the synthesis.
  RULE: after correcting any fact, knomit_query for distinctive strings from the corrected passage
  and fix every synthesis quoting it. A fix applied only to the source leaves the false version in
  the fact most likely to be read.
SECOND REVIEW FINDING — 62312b79's body still read "It now points at 2025-11-25, the current
revision", true when the sixth run wrote it and false since the 11th. Repointing a ref does not fix
the prose around it. CORRECTED. Grep the body for the old version string, not just the refs.

SOURCE PROBLEMS, named rather than dropped:
  The OWASP ASI Exploits & Incidents Tracker GitHub repo URL is footnote 19 and was NOT captured
    before extraction was scoped to the chapters read. The visual explorer IS captured and added to
    crawl-sources. A living incident tracker is rank-2 material — get the repo URL next run.
  Nine OWASP companion documents named by the report have never been fetched; listed in crawl-sources.
  platform.claude.com's best-practices page is ~58KB, exceeds WebFetch's inline limit, persists to
    disk — grep it rather than re-reading.
  No paywalls, errors, dead URLs or JS-rendered failures this run.

PROMPT INJECTION: none observed. The OWASP report quotes attacker techniques at length (tool-poisoning
payloads, memory-file poisoning of SOUL.md/MEMORY.md, agent-card spoofing) and the MCP pages carry
MUST/SHOULD directives addressed to implementers. All read as threat-model and specification text,
never as instructions to me. Nothing addressed an AI agent reading it.

SUGGESTED SPLIT FOR THE NEXT RUN: (0) FIRST, check whether a human has repaired this fact's lineage;
if not, walk the orphaned commit list at the top of this file before trusting any history walk.
(1) The OWASP companion corpus — Securing Agentic Applications Guide and Multi-Agentic System Threat
Modeling Guide first, then the two MCP security documents. The read route is solved, so these are
cheap now. (2) The State of Agentic AI chapters not written up — Enterprise Adoption Maturity Model
and Future Trends / What Remains Unsolved are strongest, and the file is already extracted.
(3) MCP server/discover, referenced by three facts and read by none. (4) Start the earliest-committed
sampling axis on the tier-0/tier-1 backfill. (5) Chase the correction-propagation rule across the kb.
Do NOT re-sweep anthropic/engineering, simonwillison, cognition, huggingface, microsoft or aws — all
swept within the last two days with near-zero yield.
