---
type: reference
domain: [agentic-engineering, claude-api, prompting, security, context-engineering, reliability]
confidence: 0.85
sources: 1
entities: [Anthropic, Claude Opus 5.5, Claude Fable 5.1, preserved thinking, distillation, extended thinking, compaction]
motifs: [account-age-gates-behaviour, extraction-defence-breaks-rewrites]
refs: ['https://www.anthropic.com/claude-opus-5-5', 'kb://bc6eac5f37df/kb/invariants/ai/agents/prompting/claude-api/ed084a84.md', 'kb://bc6eac5f37df/kb/architecture/ai/agents/context-engineering/1531a0d1.md']
---
# Preserved thinking blocks editing Claude's prior context on Fable 5.1 and Opus 5.5 — but only for API accounts created on or after 2026-08-31, so the same code works on one account and not another

Anthropic introduced "preserved thinking" with Claude Fable 5.1 as an anti-distillation safeguard and shipped it again with Claude Opus 5.5 (launched 2026-09-22). Its stated function: it "stops API users from editing Claude's prior context in an attempt to extract Claude's reasoning."

THE CONDITION IS COMPOUND AND BOTH HALVES BIND. It applies to Fable 5.1 AND Opus 5.5, AND ONLY to API accounts created on or after 2026-08-31. Neither half alone predicts the behaviour: an older account calling Opus 5.5 is not subject to it, and a new account calling an older model is not either. The practical consequence of the account-age half is the one that costs debugging time — identical client code behaves differently on two accounts at the same organisation, and a reproduction on the older account will not show the failure.

WHY IT REACHES ORDINARY AGENT CODE AND NOT ONLY ATTACKERS: editing prior assistant turns is a mainstream context-engineering technique, not an exotic one. Compaction that rewrites earlier turns into a summary, a scaffold that trims or reorders thinking blocks to fit a window, a replay harness that reconstructs a conversation from stored state — all of these edit prior context, and all of them are what this safeguard is built to reject. The motivation is extraction (distillation attacks using many fake accounts to pull a model's reasoning at industrial scale), but the mechanism does not read intent.

OPERATIONAL: if you compact, replay or rewrite assistant history against these models, test on an account created after the cutoff before assuming your pipeline works, and treat the newer account as the one that tells the truth about production. Anthropic publishes preserved-thinking documentation and a Help Center article describing how to test and update integrations; this fact does not reproduce the API-level detail of what exactly is rejected, which is not stated in the launch post.

NOT ESTABLISHED HERE: the error mode (whether an edited prior context is rejected with an error, silently ignored, or degraded), whether the cutoff will move, and whether the restriction extends to later models. The launch post states the policy, not the interface.
